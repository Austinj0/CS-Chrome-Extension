window.addEventListener('load', function () { 
    /*
    const getParamValue = (paramName, url) => {
        // Create a URL object from the provided URL
        const urlObj = new URL(url);
        
        // Use the URLSearchParams object to get the value of the parameter
        const paramValue = urlObj.searchParams.get(paramName);
        
        // Return the parameter value
        return paramValue;
    }
    setTimeout(function(){
        const sdk = document.querySelector("#alloyIframe");
        const ja = getParamValue("journeyApplicationToken", sdk.src);
        const journey = getParamValue("journeyToken", sdk.src);
        const key = getParamValue("key", sdk.src);
        const sdkDetails = {
            ja : ja,
            journey: journey,
            key: key
        }
        console.log(sdkDetails)
    }, 5000);
    */
})